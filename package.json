{
  "name": "tcp_server",
  "version": "0.0.1",
  "description": "a tcp server to push the values to mongodb",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "niketan",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^3.0.10",
    "mongoose": "^5.1.1"
  }
}
